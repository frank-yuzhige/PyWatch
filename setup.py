from setuptools import find_packages, setup

setup(
    name='pywatch',
    packages=find_packages(include=['pywatch']),
    version='0.0.7',
    description='A state management library in python that performs incremental computation. Demo version',
    author='Frank Yu',
    license='MIT',
)
